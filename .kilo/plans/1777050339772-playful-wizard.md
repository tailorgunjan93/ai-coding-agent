# Plan: Create Folder Structure for ai-coding-agent

## Goal
Create the complete folder structure as defined in `PlanningBluePrint/folderstructure.md`.

## Structure to Create

### Root Files
- `README.md` (exists)
- `planning.md` (exists)
- `LICENSE` (new)

### Main Source Directory (`src/`)
```
src/
├── main/
│   ├── agent/
│   │   └── nodes/
│   │       ├── planning_node.py
│   │       ├── human_approval_node.py
│   │       ├── model_node.py
│   │       ├── lifecycle_node.py
│   │       ├── tester_node.py
│   │       ├── db_node.py
│   │       ├── git_node.py
│   │       ├── cicd_node.py
│   │       ├── design_pattern_node.py
│   │       ├── security_node.py
│   │       └── output_node.py
│   ├── utils/
│   │   ├── code_commenter.py
│   │   ├── design_pattern_enforcer.py
│   │   ├── db_handler.py
│   │   ├── git_helper.py
│   │   ├── cicd_generator.py
│   │   └── test_runner.py
│   └── langgraph_schema.json
└── extension/
    ├── commands/
    ├── ui/
    └── manifest.json
```

### Test Directories (`tests/`)
```
tests/
├── unit/
├── integration/
├── edge_cases/
└── db_tests/
```

### Agent Graphs (`agent-graphs/`)
```
agent-graphs/
├── code_graph.json
├── data_flow_graph.json
└── visualizations/
```

### CI Directory (`ci/`)
```
ci/
├── github-actions.yml
├── azure-pipelines.yml
└── gitlab-ci.yml
```

### Documentation (`docs/`)
```
docs/
├── architecture.md
├── workflow.md
├── design_patterns.md
├── testing_strategy.md
└── ci_cd_integration.md
```

### Database (`db/`)
```
db/
├── schema.sql
├── migrations/
└── orm_models/
```

### VSCode Config (`.vscode/`)
```
.vscode/
├── settings.json
├── launch.json
└── tasks.json
```

### GitHub Config (`.github/`)
```
.github/
├── ISSUE_TEMPLATE.md
├── PULL_REQUEST_TEMPLATE.md
└── workflows/
```

## Implementation Steps

1. Create all directories using `mkdir` commands (with `-p` for nested creation)
2. Create empty placeholder files for all specified files
3. Verify the structure matches the specification

## Commands to Execute

```bash
# Create main source directories and files
mkdir -p src/main/agent/nodes
mkdir -p src/main/utils
mkdir -p src/extension/commands
mkdir -p src/extension/ui

# Create node files
type nul > src/main/agent/nodes/planning_node.py
type nul > src/main/agent/nodes/human_approval_node.py
type nul > src/main/agent/nodes/model_node.py
type nul > src/main/agent/nodes/lifecycle_node.py
type nul > src/main/agent/nodes/tester_node.py
type nul > src/main/agent/nodes/db_node.py
type nul > src/main/agent/nodes/git_node.py
type nul > src/main/agent/nodes/cicd_node.py
type nul > src/main/agent/nodes/design_pattern_node.py
type nul > src/main/agent/nodes/security_node.py
type nul > src/main/agent/nodes/output_node.py

# Create utils files
type nul > src/main/utils/code_commenter.py
type nul > src/main/utils/design_pattern_enforcer.py
type nul > src/main/utils/db_handler.py
type nul > src/main/utils/git_helper.py
type nul > src/main/utils/cicd_generator.py
type nul > src/main/utils/test_runner.py

# Create schema file
type nul > src/main/langgraph_schema.json
type nul > src/extension/manifest.json

# Create test directories
mkdir -p tests/unit
mkdir -p tests/integration
mkdir -p tests/edge_cases
mkdir -p tests/db_tests

# Create agent-graphs directory
mkdir -p agent-graphs/visualizations
type nul > agent-graphs/code_graph.json
type nul > agent-graphs/data_flow_graph.json

# Create ci directory
mkdir -p ci
type nul > ci/github-actions.yml
type nul > ci/azure-pipelines.yml
type nul > ci/gitlab-ci.yml

# Create docs directory
mkdir -p docs
type nul > docs/architecture.md
type nul > docs/workflow.md
type nul > docs/design_patterns.md
type nul > docs/testing_strategy.md
type nul > docs/ci_cd_integration.md

# Create db directory
mkdir -p db/migrations
mkdir -p db/orm_models
type nul > db/schema.sql

# Create .vscode directory
mkdir -p .vscode
type nul > .vscode/settings.json
type nul > .vscode/launch.json
type nul > .vscode/tasks.json

# Create .github directory
mkdir -p .github/workflows
type nul > .github/ISSUE_TEMPLATE.md
type nul > .github/PULL_REQUEST_TEMPLATE.md

# Create LICENSE
type nul > LICENSE
```
